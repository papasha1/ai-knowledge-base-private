# 嵌入式基础

---

## GPIO 速度 vs 传感器时序

**记忆点：** MCU 太快，传感器跟不上，加延时就行。

**掌握程度：** 🟡 理解原理，实操过一次

**场景：** 用 STM32（72MHz）读 HX711 传感器，数据跳变严重，加了 `delayMicroseconds(1)` 后解决。

**原理：** STM32 的一次 GPIO 翻转只需约 14 纳秒，但 HX711 要求时钟脉冲至少 100 纳秒。MCU 太快了，翻完 GPIO 的时候传感器还没反应过来，数据读错了。

**规律：** 高速 MCU 对接慢速传感器时，几乎 always 需要在时序操作之间加延时。看数据手册的时序图，找最小脉宽参数（t_HIGH、t_LOW），对比 MCU 的 GPIO 翻转时间。

**代码：** `FeedBowlForA/lib/hx711/hx711.cpp:29` — `HX711_DELAY()` 宏定义

**关联：** [HX711](sensors.md#hx711称重)、[I2C](protocols.md#i2c)（同样的时序思维）

**来源：** FeedBowlForA，2026-05-20

---

## 传感器预热与采样平均

**记忆点：** 上电等几秒再校准，多读几次取平均就稳了。

**掌握程度：** 🟡 理解原理，实操过一次

**场景：** HX711 上电后读数漂移，加了 2 秒预热 + 30 次采样去皮后稳定。

**原理：**
- **预热：** 模拟传感器刚上电时内部电路未稳定，输出会漂移。等几秒再校准是标准做法。
- **均值滤波：** 单次采样有随机噪声，多次采样取平均后噪声互相抵消。最简单的数字滤波方法。
- **中值滤波（进阶）：** 采 N 次排序取中间值，能排除偶发异常读数。

**代码：** `FeedBowlForA/lib/hx711/hx711.cpp:54` — `hx711_read_raw_avg()`

**关联：** [HX711](sensors.md#hx711称重)

**来源：** FeedBowlForA，2026-05-20

---

## 传感器校准的两种误差

**记忆点：** 偏移减固定值，比例除系数，两个都有就解方程。

**掌握程度：** 🟡 理解原理，实操过一次

**场景：** HX711 需要乘校准系数（比例误差），VL53L0X 需要减固定偏移（偏移误差）。

**原理：**
- **偏移误差：** 测量值 = 真实值 + 固定偏移。修正：减去偏移量。
- **比例误差：** 测量值 = 真实值 × 系数。修正：除以系数。
- **两点校准：** 两个测量点误差不同时，说明同时有两种误差，需解二元方程。

**代码：** `FeedBowlForA/include/app_config.h:39` — `FEED_WEIGHT_CAL_FACTOR` 和 `VL53L0X_OFFSET_MM`

**关联：** [HX711](sensors.md#hx711称重)、[VL53L0X](sensors.md#vl53l0xtof-测距)

**来源：** FeedBowlForA，2026-05-20

---

## 硬件抽象层（HAL）

**记忆点：** 踩油门不用管是汽油机还是电动机——接口不变，实现随便换。

**掌握程度：** 🟡 理解原理，实操过一次

**做法：** 把底层操作封装成统一接口，上层只调接口。迁移框架时只改实现，上层不动。

**原理（依赖倒置）：** 高层不应依赖低层，两者都应依赖抽象。就像开车踩油门，不用管底下是汽油机还是电动机。

**代码：** `FeedBowlForA/lib/hal/hal_gpio.h` — GPIO 抽象接口定义

**关联：** [PlatformIO](tools.md#platformio)（目录结构配合 HAL 分层）

**来源：** FeedBowlForA，2026-05-20
